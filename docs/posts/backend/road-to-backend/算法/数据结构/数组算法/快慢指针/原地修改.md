---
title: '原地修改'
date: 2024-05-08
updated: '2024-05-08 07:02:06'
category: 'backend'
tags:
  - '后端修仙之路'
  - '算法'
  - '数据结构'
lastUpdated: false
summary: '就是不创建新数组的情况下，删除重复元素或者是删除指定的数，必须保证原有的数组顺序。这时就需要用到快慢指针了，fast用于遍历，slow用于指向赋值的下个位置'
source: '语雀导出'
source_url: 'https://www.yuque.com/pohongying/cnhh4c/ngmsq8i1gh1r02p9'
---

# 原地修改

### 原地修改是什么意思呢
就是不创建新数组的情况下，删除重复元素或者是删除指定的数，必须保证原有的数组顺序。这时就需要用到快慢指针了，fast用于遍历，slow用于指向赋值的下个位置

删除重复元素，要注意第一个元素可能是不能删除的，所以呢，要先slow++，因此它的位置也是元素的索引位置

移除元素，dang'q要先设置元素后slow++，，因此个数就是当前的位置

### 举例
```java
class Solution {
    public int removeDuplicates(int[] nums) {
        if (nums.length == 0) {
            return 0;
        }
        int slow = 0, fast = 0;
        while (fast < nums.length) {
            if (nums[fast] != nums[slow]) {
                // 维护 nums[0..slow] 无重复
                slow++;
                nums[slow] = nums[fast];
            }
            fast++;
        }
        // 数组长度为索引 + 1
        return slow+1;
    }
}
```

```java
class Solution {
    public int removeElement(int[] nums, int val) {
        int n = nums.length;
        int fast = 0;
        int slow = 0;

        while (fast < n) {
            if (nums[fast] != val) {
                nums[slow] = nums[fast];
                slow++;
            }
            fast++;
        }
        return slow;
    }
}
```

---

> 更新: 2026-01-22 02:24:32  
> 来源: 语雀导出
