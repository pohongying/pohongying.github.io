---
title: '归并排序'
date: 2026-01-25
updated: '2026-01-25 13:44:56'
category: 'backend'
tags:
  - '后端修仙之路'
  - '算法'
  - '十大排序'
lastUpdated: false
summary: '后序遍历，nlog(n)很稳定'
source: '语雀导出'
source_url: 'https://www.yuque.com/pohongying/cnhh4c/fw6w6zfgqdvm694r'
---

# 归并排序

后序遍历，nlog(n)很稳定

#### 核心原理
1. **分（Divide）：** 把数组从中间一分为二，一直分到每个部分只剩 1 个元素（此时它自然有序）。
2. **治（Conquer/Merge）：** 把两个有序的小数组，合并成一个大的有序数组。

#### 代码
```java
public class MergeSort {
    public static void mergeSort(int[] arr, int left, int right) {
        if (left >= right) return; // 递归出口

        int mid = (left + right) / 2;
        // 1. 先分：递归拆分左右
        mergeSort(arr, left, mid);
        mergeSort(arr, mid + 1, right);

        // 2. 后合：合并两个有序区间
        merge(arr, left, mid, right);
    }

    // 合并操作（核心逻辑）
    private static void merge(int[] arr, int left, int mid, int right) {
        int[] temp = new int[right - left + 1]; // 临时数组
        int i = left, j = mid + 1, k = 0;

        // 谁小移谁
        while (i <= mid && j <= right) {
            temp[k++] = (arr[i] <= arr[j]) ? arr[i++] : arr[j++];
        }

        // 处理剩余元素
        while (i <= mid) temp[k++] = arr[i++];
        while (j <= right) temp[k++] = arr[j++];

        // 搬回原数组
        for (int p = 0; p < temp.length; p++) {
            arr[left + p] = temp[p];
        }
    }
}
```

---

> 更新: 2025-04-30 22:43:52  
> 来源: 语雀导出
